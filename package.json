{
  "name": "vessel-state",
  "version": "1.0.0-beta13",
  "description": "Takes a stream of data and decodes it into a reactive state for use in Vue components.",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "yarn run clean && vue-cli-service build --target lib src/main.ts && tsc --emitDeclarationOnly",
    "publish": "yarn publish",
    "publish:beta": "yarn publish --tag beta",
    "test": "cross-env TS_NODE_TRANSPILE_ONLY=true NODE_ENV=test mocha --ui bdd --recursive --require ts-node/register test/**/*.test.ts",
    "test:watch": "yarn run test --watch --watch-extensions ts --watch-files src,test --reporter min"
  },
  "main": "dist/vessel-state.common.js",
  "module": "dist/vessel-state.umd.js",
  "typings": "dist/src/main.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "extended-nmea": "2.0.0-beta11",
    "vue": "^2.6.12",
    "vuex": "^3.6.2"
  },
  "devDependencies": {
    "@types/node": "^15.0.2",
    "@vue/cli-plugin-typescript": "~4.5.0",
    "@vue/cli-service": "^4.5.13",
    "chai": "^4.3.4",
    "cross-env": "^7.0.3",
    "mocha": "^8.4.0",
    "rimraf": "^3.0.2",
    "ts-loader": "~8.2.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4",
    "vue-template-compiler": "^2.6.11"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "authors": [
    "Ricardo Boss <contact@ricardoboss.de>"
  ],
  "license": "MIT",
  "repository": "https://github.com/ricardoboss/vessel-state",
  "types": "dist/index.d.ts"
}
